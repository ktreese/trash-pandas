# ─────────────────────────────────────────────
# Trash Pandas 14U — environment variables
# Copy this file to .env.local and fill in values
# ─────────────────────────────────────────────

# Vercel Blob — the only service you need!
# Option A (easiest): run `vercel link` then `vercel env pull .env.local`
# Option B (manual): vercel.com → Storage → Blob → your store → .env.local tab
BLOB_READ_WRITE_TOKEN="vercel_blob_rw_xxxxxxxxxxxxxxxxxxxx"
